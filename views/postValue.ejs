<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>게시물 목록</h3>

    <ul>
        <% titles.forEach((title, index) => { %>
            <li><a href="/post/<%= index + 1 %>"><%= title %></a></li>
        <% }); %>
    </ul>
</body>
</html>